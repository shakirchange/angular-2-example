import { AppHighletDirective } from './app-highlet.directive';

describe('AppHighletDirective', () => {
  it('should create an instance', () => {
    const directive = new AppHighletDirective();
    expect(directive).toBeTruthy();
  });
});
